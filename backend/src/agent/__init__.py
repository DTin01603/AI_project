"""LangGraph agent package.

Intentionally empty: importing submodules directly avoids triggering the
optional LLM SDK chain in environments that only need a subset of the
package (e.g. unit tests that exercise a single node).
"""
